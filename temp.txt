 var sub = expense.reduce(function(a,b){
          return a + b;
        })
        const balance = sum - sub;
        User.findByIdAndUpdate(req.params.id,{
          balance : balance
        })
        .then(data => {
          res.send(data)
        })

 var sum = income.reduce(function(a,b){
        return a + b;
      })